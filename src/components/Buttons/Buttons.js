import React from 'react';
// import './Buttons.css';

const Button = () => {
    return (
        <div>
            <button className="bold" type='submit'>Important</button>
            <button  type='submit'>Not Important</button>
        </div>
    );
};

export default Button;
